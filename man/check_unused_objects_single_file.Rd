% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_unused_objects_single_file.R
\name{check_unused_objects_single_file}
\alias{check_unused_objects_single_file}
\title{Check for objects that are created but never used within a file}
\usage{
check_unused_objects_single_file(path)
}
\arguments{
\item{path}{Character. Path to a file.}
}
\value{
A data frame with one column, \code{unused_objects}, listing orphaned objects.
Returns an empty data frame if no orphans are found.
}
\description{
Parses an R script or an R/Quarto document, extracts the R code, and reports
symbols that are assigned to but never referenced anywhere else in the same file.
Supports \code{.R}, \code{.Rmd}, and \code{.qmd} (R code chunks only for the latter two).
}
\examples{
path <- tempfile(fileext = ".R")
writeLines(
  c(
    "df1 <- data.frame(x = 1:3)",
    "df2 <- transform(df1, y = x * 2) # never used",
    "print(df1)"
  ),
  path
)
check_unused_objects_single_file(path)
unlink(path)

}
