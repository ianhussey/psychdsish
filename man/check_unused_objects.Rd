% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_unused_objects.R
\name{check_unused_objects}
\alias{check_unused_objects}
\title{Check all project files for objects that are created but never used}
\usage{
check_unused_objects(
  root = ".",
  patterns = c("\\\\.R$", "\\\\.r$", "\\\\.Rmd$", "\\\\.rmd$", "\\\\.qmd$", "\\\\.Qmd$"),
  exclude_dirs = c(".git", "renv", "_site", "_freeze", ".quarto", "_book", "tools"),
  verbose = FALSE
)
}
\arguments{
\item{root}{Character scalar. Directory to search from (default \code{"."}).}

\item{patterns}{Character vector of regexes for file extensions to include.
Defaults to common R and Quarto suffixes.}

\item{exclude_dirs}{Character vector of directory names to skip anywhere in
the path (default: \code{c(".git","renv","_site","_freeze",".quarto","_book","tools")}).}

\item{verbose}{Logical; if \code{TRUE}, message progress (default \code{FALSE}).}
}
\value{
A data frame with columns:
\describe{
\item{file}{File path (character).}
\item{unused_objects}{Name of an object that is created but never used (character).}
}
One row per unused object. If none are found, returns an empty data frame
with the two columns.
}
\description{
Recursively searches for R/Quarto files and applies
\code{check_unused_objects_single_file()} to each, collating results.
}
\details{
Supported extensions: \code{.R}, \code{.r}, \code{.Rmd}, \code{.rmd}, \code{.qmd}, \code{.Qmd}.
}
\examples{
\dontrun{
# Scan the current project
check_unused_objects(".")

# Show what it finds in a subdirectory
check_unused_objects("R")
}
}
