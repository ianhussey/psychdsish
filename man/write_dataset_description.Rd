% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_dataset_description.R
\name{write_dataset_description}
\alias{write_dataset_description}
\title{Write a psych-DS dataset_description.json from the project's codebooks}
\usage{
write_dataset_description(
  project_root,
  name = NULL,
  description = NULL,
  quiet = FALSE
)
}
\arguments{
\item{project_root}{Character scalar. Path to the project root, e.g.,
\code{"../"} from \code{code/processing.qmd}.}

\item{name}{Character scalar. The dataset's name. Defaults to the name in
an existing \code{dataset_description.json}, or the project folder's name.}

\item{description}{Character scalar. A description of the dataset.
Defaults to the description in an existing file, or a placeholder to
complete by hand.}

\item{quiet}{Logical. If \code{FALSE} (default), a one-line summary is printed
as a message.}
}
\value{
(Invisibly) a list with \code{path}, \code{name}, \code{description}, and \code{variables}
(the variable names written).
}
\description{
Creates or updates \code{dataset_description.json} in the project root, which
the \href{https://psych-ds.github.io/}{psych-DS} standard requires. The
variables it describes are taken from the codebooks created by the codebook
chunk in \code{code/processing.qmd}, so that each variable is described once, in
the codebook, rather than twice.
}
\details{
Documenting variables in a codebook \code{.csv} is the simpler option, and is
all that \code{\link[=validator]{validator()}} requires. Writing them to
\code{dataset_description.json} as well is more work, but makes the project
closer to a valid psych-DS dataset, which is machine-readable and easier
for others to reuse. Use whichever suits the project; you do not need both.

Each codebook row becomes a \code{PropertyValue} entry in \code{variableMeasured},
mapping the codebook's \code{variable}, \code{description}, and \code{units} columns onto
\code{name}, \code{description}, and \code{unitText}. A \code{coding} entry that is not
"none" is appended to the description, because psych-DS has no separate
field for it. Where the same variable appears in more than one codebook,
the first description is used.

Fields that the file already contains and that this function does not
write (e.g., \code{author} or \code{license}) are kept.

Data file names in the templates follow the psych-DS convention of
\code{key-value} pairs ending in \verb{_data}, e.g.,
\verb{study-1_stage-processed_data.csv}. To check a project against the
standard itself, use the psych-DS validator
(\url{https://psych-ds.github.io/validator/}).
}
\examples{
project <- file.path(tempdir(), "psychds_project")
create_project_skeleton(project, quiet = TRUE)

# normally written by the codebook chunk in code/processing.qmd
write.csv(
  data.frame(
    variable = c("id", "rt"),
    description = c("Participant identifier", "Response time"),
    units = c("none", "milliseconds"),
    coding = c("none", "none")
  ),
  file.path(project, "data", "processed", "stage-processed_codebook.csv"),
  row.names = FALSE
)

write_dataset_description(project, name = "My study", description = "A demo")
cat(readLines(file.path(project, "dataset_description.json")), sep = "\n")

unlink(project, recursive = TRUE)

}
\seealso{
\code{\link[=create_project_skeleton]{create_project_skeleton()}}, \code{\link[=validator]{validator()}}
}
