% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_all_files.R
\name{style_all_files}
\alias{style_all_files}
\title{Style All Code in R, R Markdown, and Quarto Files in a Project}
\usage{
style_all_files(
  root = ".",
  patterns = c("\\\\.R$", "\\\\.r$", "\\\\.Rmd$", "\\\\.rmd$", "\\\\.qmd$", "\\\\.Qmd$"),
  exclude_dirs = c(".git", "renv", "_site", "_freeze", ".quarto", "_book", "tools"),
  dry_run = FALSE,
  ...
)
}
\arguments{
\item{root}{Character scalar. Path to the root directory to search
(default: current working directory \code{"."}).}

\item{patterns}{Character vector of regular expressions for file extensions
to include. Defaults to common R and Quarto file suffixes.}

\item{exclude_dirs}{Character vector of directory names to skip (default:
\code{.git}, \code{renv}, \verb{_site}, \verb{_freeze}, \code{.quarto}, \verb{_book}, \code{tools}).
Matching is done anywhere in the path.}

\item{dry_run}{Logical. If \code{TRUE}, returns the list of files that would be
styled but does not modify them (default: \code{FALSE}).}

\item{...}{Additional arguments passed on to \code{\link[styler:style_file]{styler::style_file()}}, such as
\code{strict = FALSE}.}
}
\value{
Invisibly returns the vector of file paths that were styled.
If \code{dry_run = TRUE}, returns the vector of candidate file paths without
styling.
}
\description{
Recursively finds \code{.R}, \code{.Rmd}, and \code{.qmd} files under a directory and
restyles them using \code{\link[styler:style_file]{styler::style_file()}}. This includes both code scripts
and R code chunks embedded in R Markdown or Quarto documents. Certain
directories can be excluded from the search.
}
\details{
\itemize{
\item The search is recursive, starting at \code{root}.
\item Excluded directories are filtered out using a regex match anywhere in the
path.
\item If no matching files are found, a message is issued and an empty character
vector is returned invisibly.
}
}
\examples{
\dontrun{
# Preview which files would be styled
style_all_files(dry_run = TRUE)

# Style all files under the current project
style_all_files()
}

}
\seealso{
\code{\link[styler:style_file]{styler::style_file()}}, \code{\link[styler:style_dir]{styler::style_dir()}}, \code{\link[usethis:tidyverse]{usethis::use_tidy_style()}}
}
