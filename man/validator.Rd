% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validator.R
\name{validator}
\alias{validator}
\title{Validate a project directory structure against psych-ds-ish requirements}
\usage{
validator(project_root = "../", strict = FALSE)
}
\arguments{
\item{project_root}{Character scalar. Path to the project root directory.
Defaults to \code{"../"}. The path is normalized internally.}

\item{strict}{Logical. If \code{TRUE}, throw an error when any check fails,
listing the failed checks and how to fix them. Useful in continuous
integration (e.g., GitHub Actions), where the error makes the job fail.
Defaults to \code{FALSE}.}
}
\value{
A tibble of class \code{psychdsish_validation} with one row per check
(failures first, then skipped checks), containing:
\describe{
\item{Test}{Description of the check performed.}
\item{Status}{\code{"PASS"}, \code{"FAIL"}, or \code{"SKIP"} (the check could not be
run).}
\item{Details / Guidance}{Additional information, such as instructions
for fixing failures or offending file paths.}
}
Printing it shows coloured PASS/FAIL results with guidance for failures.
\code{summary()} returns a list with \code{project_root}, \code{n_pass}, \code{n_fail},
\code{n_skip}, and \code{passed} (\code{TRUE} if no check failed). If \code{strict = TRUE} and any check
fails, an error is thrown instead.
}
\description{
This function checks whether a project directory follows a defined set
of structural and file-placement rules. The layout is loosely inspired by the
\href{https://psych-ds.github.io/}{psych-DS} specification, but is \strong{not}
fully psych-DS compliant. It adds extra conventions for reproducibility
and common analysis workflows, including Quarto file templates and
a \code{.gitignore} file. It does not comply with psych-ds's .json requirement.
}
\details{
The validator performs several categories of checks:
\itemize{
\item \strong{Required directories} — Ensures that expected top-level and
nested folders exist (e.g., \verb{code/}, \verb{data/raw/}, \verb{methods/}).
\item \strong{Required files} — Confirms presence of a \code{readme.md} and
a license file (e.g., \code{LICENSE}).
\item \strong{Filetype-location constraints} — Ensures specific file types
(e.g., \code{.qmd}, \code{.csv}, \code{.rds}, \code{.png}) only appear in approved
subdirectories and not elsewhere in the project.
\item \strong{Special HTML rule} — All \code{.html} files must be under \verb{code/}
or \verb{methods/}.
\item \strong{Hygiene checks} — Optional rules that discourage spaces in
filenames, check for \code{.gitignore} presence, and prevent data
files being stored under \verb{code/}.
\item \strong{Reproducibility checks} — Raw data files have not been modified
or deleted since they were first committed to git (including
uncommitted changes); rendered \code{.html} files are newer than their
\code{.qmd} (using git commit times for committed, unmodified files, and
file modification times otherwise); \code{README.md} no longer contains
the template placeholders written by \code{\link[=create_project_skeleton]{create_project_skeleton()}};
and R code in \code{.R} files and in \code{.qmd}/\code{.Rmd} code chunks contains
no \code{setwd()} calls or absolute file paths (e.g., \code{"~/"},
\code{"/Users/"}, \code{"C:/"}). Comment lines are ignored.
}

Checks that cannot be run are reported as \code{"SKIP"} rather than \code{"PASS"},
e.g., the raw data check when the project is not a git repository, or the
\code{.html} check when nothing has been rendered yet. Skipped checks do not
cause \code{strict = TRUE} to fail.
}
\examples{
\dontrun{
# Run validation on the parent directory
results <- validator("../")

# Pretty-print results in the console
print(results)

# Overall result
summary(results)$passed

# Fail a CI job (e.g., GitHub Actions) if any check fails
validator(".", strict = TRUE)

# Display results as a styled HTML table
library(knitr)
library(kableExtra)
results |>
  knitr::kable() |>
  kableExtra::kable_classic(full_width = FALSE)
}

}
